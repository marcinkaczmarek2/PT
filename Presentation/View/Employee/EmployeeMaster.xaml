<UserControl x:Class="Presentation.View.Employee.EmployeeMaster"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Presentation.ViewModel">
    <UserControl.DataContext>
        <vm:VMEmployeeList />
    </UserControl.DataContext>

    <Border Background="White" Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Text="Employees" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="3*" />
                </Grid.ColumnDefinitions>

                <!-- Lista pracownikw -->
                <ListBox ItemsSource="{Binding Employees}" SelectedItem="{Binding SelectedEmployee}" DisplayMemberPath="Name" 
                         Margin="0,0,10,0" FontSize="16" />

                <!-- Szczegy i edycja -->
                <StackPanel Grid.Column="1" Margin="10" VerticalAlignment="Top">
                    <Label Content="Name:" />
                    <TextBox Text="{Binding SelectedEmployee.Name}" Margin="0,0,0,10"/>

                    <Label Content="Position:" />
                    <TextBox Text="{Binding SelectedEmployee.Position}" Margin="0,0,0,10"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                        <Button Content="Add" Command="{Binding AddCommand}" Width="60" Margin="0,0,10,0"/>
                        <Button Content="Remove" Command="{Binding RemoveCommand}" Width="80"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</UserControl>
