<UserControl x:Class="Presentation.View.Book.BookMaster"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Presentation.ViewModel"
             Background="White">
    <UserControl.DataContext>
        <vm:VMBookList />
    </UserControl.DataContext>

    <Border Background="White" Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Text="Books" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="3*" />
                </Grid.ColumnDefinitions>

                <!-- Lista ksiek -->
                <ListBox ItemsSource="{Binding Books}" SelectedItem="{Binding SelectedBook}" DisplayMemberPath="Title" 
                         Margin="0,0,10,0" FontSize="16" />

                <!-- Szczegy -->
                <StackPanel Grid.Column="1" Margin="10" VerticalAlignment="Top">
                    <Label Content="Title:" />
                    <TextBox Text="{Binding SelectedBook.Title}" Margin="0,0,0,10"/>

                    <Label Content="Author:" />
                    <TextBox Text="{Binding SelectedBook.Author}" Margin="0,0,0,10"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                        <Button Content="Add" Command="{Binding AddCommand}" Width="60" Margin="0,0,10,0"/>
                        <Button Content="Remove" Command="{Binding RemoveCommand}" Width="80"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</UserControl>
